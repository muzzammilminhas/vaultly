package com.muzzammil.vaultly

import io.flutter.embedding.android.FlutterFragmentActivity

// local_auth's Android implementation requires the hosting activity to be a
// FlutterFragmentActivity (it needs a FragmentActivity to show the biometric prompt).
class MainActivity : FlutterFragmentActivity()
